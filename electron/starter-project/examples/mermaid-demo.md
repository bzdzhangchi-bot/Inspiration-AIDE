# Mermaid 预览示例

这个文件用来确认 Markdown 预览和 Mermaid 渲染都正常工作。

```mermaid
flowchart LR
  A[打开 Inspiration] --> B[进入 Starter Project]
  B --> C[阅读 新手引导.md]
  C --> D[打开 Terminal 试命令]
  D --> E[配置 Settings]
  E --> F[开始在 Chat 中协作]
```

你可以试着：

- 修改节点名称
- 新增一个分支
- 把 `LR` 改成 `TD`

这样能立刻看到 Markdown 预览的联动效果。
